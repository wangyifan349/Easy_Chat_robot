# 用于字符串模糊匹配的库
fuzzywuzzy>=0.18.0  # 提供编辑距离和相似度计算功能

# 用于文本特征提取的库
scikit-learn>=1.0.2  # 提供 TF-IDF 向量化和余弦相似度计算功能

# 用于数值计算的库
numpy>=1.21.2  # 提供高效的数组操作和数值计算功能

# 如果需要使用 fuzzywuzzy 的快速版本，可以安装 python-Levenshtein
# python-Levenshtein>=0.12.2  # 提供更快的编辑距离计算

# 其他可选的字符串相似度计算库
rapidfuzz>=1.0.0  # 提供快速的字符串相似度计算，替代 fuzzywuzzy
textdistance>=4.2.1  # 提供多种字符串距离和相似度算法的实现
difflib>=3.7.4  # Python 内置库，提供简单的序列比较功能

# 如果需要进行自然语言处理，可以考虑安装以下库
nltk>=3.6.3  # 自然语言处理工具包，提供文本处理和分析功能
spacy>=3.1.0  # 高效的自然语言处理库，支持多种语言
